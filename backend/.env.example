# ============================================================================
# MoodDJ Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# ============================================================================
# LOCAL DEVELOPMENT CONFIGURATION
# ============================================================================
# Use these settings when running locally on your machine

# Spotify OAuth (Development)
# Get these from: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
SPOTIFY_REDIRECT_URI=http://127.0.0.1:5000/api/auth/callback

# RapidAPI Configuration (Required for audio features)
# Get your key from: https://rapidapi.com/
RAPIDAPI_KEY=your_rapidapi_key_here
RAPIDAPI_HOST=track-analysis.p.rapidapi.com

# Database Configuration (Local)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_database_password
DB_NAME=mooddj

# Flask Configuration (Development)
SECRET_KEY=your-secret-key-change-this-in-production
FLASK_ENV=development

# URLs (Development)
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://127.0.0.1:5000

# Session Configuration (Development)
SESSION_COOKIE_SECURE=False
SESSION_COOKIE_SAMESITE=Lax

# ============================================================================
# PRODUCTION CONFIGURATION (AWS/GCP/Azure)
# ============================================================================
# Use these settings when deploying to cloud

# Spotify OAuth (Production)
# SPOTIFY_CLIENT_ID=your_spotify_client_id_here
# SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
# SPOTIFY_REDIRECT_URI=https://your-domain.com/api/auth/callback

# RapidAPI Configuration (Production - same as dev)
# RAPIDAPI_KEY=your_rapidapi_key_here
# RAPIDAPI_HOST=track-analysis.p.rapidapi.com

# Database Configuration (Production)
# DB_HOST=your-cloud-db-host.amazonaws.com
# DB_PORT=3306
# DB_USER=your_db_user
# DB_PASSWORD=your_strong_db_password
# DB_NAME=mooddj

# Flask Configuration (Production)
# SECRET_KEY=generate-a-strong-random-secret-key-here
# FLASK_ENV=production

# URLs (Production)
# FRONTEND_URL=https://your-frontend-domain.com
# BACKEND_URL=https://your-backend-domain.com

# Session Configuration (Production)
# SESSION_COOKIE_SECURE=True
# SESSION_COOKIE_SAMESITE=Lax

# ============================================================================
# IMPORTANT NOTES
# ============================================================================

# 1. Spotify Developer Dashboard Setup:
#    - Add redirect URIs for both local and production:
#      Local: http://127.0.0.1:5000/api/auth/callback
#      Production: https://your-domain.com/api/auth/callback
#
# 2. Security:
#    - Generate strong SECRET_KEY: python -c "import secrets; print(secrets.token_hex(32))"
#    - Never commit .env to git
#    - Use environment-specific .env files
#
# 3. Database:
#    - Ensure database and tables are created before running
#    - Run migrations if applicable
#
# 4. RapidAPI:
#    - Sign up at rapidapi.com
#    - Subscribe to "Track Analysis" API
#    - Copy your API key
#
# 5. Testing:
#    - Test OAuth flow works locally first
#    - Add test users in Spotify Dashboard (Development Mode)
#    - Request Extended Quota Mode for production (unlimited users)
