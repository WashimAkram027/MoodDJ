# Production Environment Configuration Template
# This file shows required environment variables for production deployment
# Actual values are stored in AWS Secrets Manager and injected by ECS

# Flask Configuration
FLASK_ENV=production
SECRET_KEY=${SECRET_KEY}  # From Secrets Manager

# Database Configuration (RDS)
DB_HOST=${RDS_ENDPOINT}  # From Terraform output
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=${DB_PASSWORD}  # From Secrets Manager
DB_NAME=mooddj

# Spotify OAuth Configuration
SPOTIFY_CLIENT_ID=${SPOTIFY_CLIENT_ID}  # Plain text, not sensitive
SPOTIFY_CLIENT_SECRET=${SPOTIFY_CLIENT_SECRET}  # From Secrets Manager
SPOTIFY_REDIRECT_URI=https://api.yourdomain.com/api/auth/callback

# RapidAPI Configuration
RAPIDAPI_KEY=${RAPIDAPI_KEY}  # From Secrets Manager
RAPIDAPI_HOST=track-analysis.p.rapidapi.com

# Application URLs (Production)
FRONTEND_URL=https://app.yourdomain.com
BACKEND_URL=https://api.yourdomain.com

# Session Configuration (Redis for production)
SESSION_TYPE=redis
SESSION_REDIS=redis://${ELASTICACHE_ENDPOINT}:6379  # From Terraform output
SESSION_COOKIE_SECURE=True  # HTTPS only
SESSION_COOKIE_SAMESITE=Lax
