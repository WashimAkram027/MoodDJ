# Production Environment Configuration
# These values are baked into the React build at build time

# For Local Docker Testing (CURRENTLY ACTIVE):
REACT_APP_API_URL=http://127.0.0.1:5000
REACT_APP_WS_URL=http://127.0.0.1:5000

# For AWS Production (uncomment when deploying to AWS, comment localhost above):
# REACT_APP_API_URL=https://api.yourdomain.com
# REACT_APP_WS_URL=https://api.yourdomain.com
